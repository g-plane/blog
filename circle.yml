machine:
  node:
    version: 8

dependencies:
  override:
    - yarn
  cache_directories:
    - ~/.cache/yarn

test:
  override:
    - yarn run deploy
  post:
    - chmod +x ./deploy.sh
    - ./deploy.sh
